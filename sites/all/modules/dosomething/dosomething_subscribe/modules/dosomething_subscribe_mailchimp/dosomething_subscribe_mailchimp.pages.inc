<?php

/**
 * @file
 * Page callbacks for dosomething subscribe mailchimp module.
 */

/**
 *  Mailchimp events webhook callback
 **/
function dosomething_subscribe_mailchimp_events() {
  
  //  GOOD THINGS WILL HAPPEN HERE.  I ASSURE YOU.

  // $event = array(
  //   'type' => $_REQUEST['type'],
  //   'fired_at' => $_REQUEST['fired_at'],
  //   'data' =>  $_REQUEST['data']
  // );

  // $email = $event['data']['email'];
  
  // // Check if a user account exists for the submitted email
  // $user = user_load_by_mail($email);

  // if (!$user) {
  //   print_r('User account not found');
  // }
  // else {
  //   print_r('User account found');
  //   print_r($user);
  // }

  exit;
}