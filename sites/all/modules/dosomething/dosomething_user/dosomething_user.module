<?php

/**
 * @file
 * dosomething_user.module
 **/

/**
 * Form constructor for the user profile school field form.
 *
 * @see dosomething_user_school_form_submit()
 *
 * @ingroup forms
 */
function dosomething_user_school_form($form, &$form_state, $nid = NULL) {
  $form = array();
  drupal_add_library('ds_neue', 'ds-neue');
  $form['#attached']['js'][] = drupal_get_path('module', 'ds_neue') . '/js/location-finder.js';;
  $form['header'] = array(
    '#markup' => '<h1>What school do you go to?</h1>',
  );
  $form['location_finder'] = array(
    '#markup' => theme('ds_neue_location_finder', array(
        'help_text' => t('Enter the zip code of your school.'),
        'results_text' => t('Nearest schools for zipcode:'),
      )
    ),
  );
  $form['school_id'] = array(
    '#title' => 'School ID',
    '#description' => 'This field will be hidden, and pre-populated with the Location Finder results',
    '#type' => 'textfield',
  );
  $form['no_school'] = array(
    '#type' => 'radios',
    '#prefix' => '<hr><p>Or:</p>',
    '#options' => array(
      0 => t('My school is not listed.'),
      1 => t('I am not affiliated with a school.'),
    ),
  );
  $form['nid'] = array(
    '#type' => 'hidden',
    '#value' => $nid,
  );
  $form['submit'] = array(
    '#type' => 'submit',
    '#value' => t('Update My Information'),
    '#attributes' => array('class' => array('btn', 'primary')),
  );
  return $form;
}

/**
 * Form submission handler for dosomething_user_school_form().
 *
 * @see dosomething_user_school_form()
 */
function dosomething_user_school_form_submit(&$form, &$form_state) {
  global $user;
  $school_id = check_plain($form_state['values']['school_id']);
  $nid = check_plain($form_state['values']['nid']);
  $no_school = $form_state['values']['no_school'];
  // If school_id, write it to profile.
  if (!empty($school_id)) {
    $profile = profile2_load_by_user($user, 'main');
    $profile->field_school_reference[LANGUAGE_NONE][0]['target_id'] = $school_id;
    $profile->save();
  }
  // @todo: Update signups table that user has been prompted for school for this campaign nid.

  // Tell the browser to close the modal.
  // @see dosomething_modal_form_builder() for where this is executed.
  $form_state['ajax_commands'][] = ctools_modal_command_dismiss();
}

/**
 * Implements hook_user_insert().
 */
function dosomething_user_user_insert(&$edit, $account, $category) {
  if (($library = libraries_load('dsapi')) && !empty($library['loaded'])) {
    // All of this will be abstracted properly.  Stubbing for memory
    // $dsapi_client = new DSAPI(
    //   variable_get('DSAPI_ACCESS_TOKEN', '') // Set this guy in settings.php from env var
    // );
    // $users_client = new UsersResource($dsapi_client);
    // $users_client->create(array());
  }
}

